// app/dashboard/projects/page.tsx
import { apiClient } from '@/lib/axiosInstance';

export default async function ProjectsPage() {
  // This page will be handled by the AdminDashboard component
  // which already includes the ProjectForm with the New Project button
  return null;
}
